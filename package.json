{
  "name": "claude-code-learnable-deck",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fixed-round builder-critic slide deck generation pipeline for Claude Code and local Ollama models.",
  "scripts": {
    "build": "tsc --noEmit",
    "make-slides": "tsx scripts/orchestrate.ts",
    "render": "tsx scripts/render_marp.ts",
    "check": "tsx scripts/check_all.ts",
    "select-best": "tsx scripts/select_best_round.ts",
    "smoke": "tsx scripts/orchestrate.ts --input examples/sample.md --deck smoke --rounds 2 && tsx scripts/check_all.ts --deck smoke"
  },
  "dependencies": {
    "dotenv": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@marp-team/marp-cli": "latest",
    "@types/node": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
